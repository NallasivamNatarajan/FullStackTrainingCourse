import { PlaceOrderPipe } from './place-order.pipe';

describe('PlaceOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
