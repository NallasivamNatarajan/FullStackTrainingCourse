import { EventDateDirective } from './event-date.directive';

describe('EventDateDirective', () => {
  it('should create an instance', () => {
    const directive = new EventDateDirective();
    expect(directive).toBeTruthy();
  });
});
