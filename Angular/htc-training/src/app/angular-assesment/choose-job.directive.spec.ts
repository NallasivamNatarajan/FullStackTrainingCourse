import { ChooseJobDirective } from './choose-job.directive';

describe('ChooseJobDirective', () => {
  it('should create an instance', () => {
    const directive = new ChooseJobDirective();
    expect(directive).toBeTruthy();
  });
});
