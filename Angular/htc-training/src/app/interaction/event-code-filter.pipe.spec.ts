import { EventCodeFilterPipe } from './event-code-filter.pipe';

describe('EventCodeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventCodeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
